{
  "name": "challenge-vita-wallet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write ."
  },
  "private": true,
  "devDependencies": {
    "cypress": "^14.5.2",
    "prettier": "^3.6.2"
  }
}
